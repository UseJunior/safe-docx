/** CSS for HTML attachment previews: auto-sized iframes and inline rendering. */
export declare const ATTACHMENT_PREVIEW_CSS: string;

/** Browser-side JS function that auto-sizes HTML attachment iframes to match content height. */
export declare const AUTO_SIZE_HTML_ATTACHMENT_FRAMES_JS: string;

/** Browser-side JS function that converts inline HTML source into sandboxed iframes for visual preview. */
export declare const CONVERT_INLINE_HTML_TO_IFRAME_JS: string;
