import type { ReconstructionMode } from './compare-types.js';

/** @internal Default retained only by the legacy rollback pipeline. */
export const DEFAULT_RECONSTRUCTION_MODE: ReconstructionMode = 'inplace';
