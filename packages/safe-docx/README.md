# @usejunior/safe-docx

Canonical npm package for the Safe Docx MCP server.

This package is the install name intended for end users:

```bash
npx -y @usejunior/safe-docx
```

Implementation is provided by `@usejunior/docx-mcp`; this package re-exports its public API and CLI entrypoint under the canonical name.
