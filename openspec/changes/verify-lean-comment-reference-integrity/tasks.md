## 1. Lean selection and semantic proof

- [x] 1.1 Add `CommentReferenceIntegrity/Semantics.lean` with the exact
  independent predicates, structural-recursive spec helpers, signatures, and
  negative witnesses pinned by the design, including semantic-predicate proofs
  rejecting `semanticEvidence = none`, substituted retained scan evidence, and
  package-view/index/bytes disagreement with the retained package record.
- [x] 1.2 Select Comments only from fixed-main direct relationship records,
  proving `none` iff zero exact-type records, `some` iff one admissible internal
  record, and typed error otherwise; include an unreferenced selected part and
  forged-absence negative witness.
- [x] 1.3 Build each comment source set from retained main, canonical selected
  header/footer physical stories, and present semantic footnote/endnote stories;
  prove no omission, duplicate, injected source, or runtime XML rescan.
- [x] 1.4 Reuse bounded `ST_DecimalNumber` parsing for all reference and direct
  definition IDs; reject non-direct definitions and canonical duplicates.
- [x] 1.5 Implement exact-one package-local resolution while allowing unique
  unreferenced definitions.
- [x] 1.6 Implement side-wide zero-evidence failure for incomplete prerequisites,
  failed selected presence, missing required relationship, and semantic limit
  crossings, with exact global first-crossing causes.
- [x] 1.7 Prove the seven exact byte-native empty-axiom semantic targets,
  including typed package/index/extraction/XML-event/one-scan relations; prove
  the five exact-foundation executable UTF-8 refinement bridges and the
  exact-foundation production refinement; and pass exact-signature,
  module-provenance recursive closure, missing-required, forbidden-extra, and
  no-LeanSpike audits.
- [x] 1.8 Integrate the Comments part into existing per-part, per-side, and
  triple compressed/expanded/ratio/depth/event budgets with metadata-first
  admission and no later work after the first global resource crossing.

## 2. Private protocol v6 and public certificate v1

- [x] 2.1 Migrate the private request/response and checker identity to v6;
  retain all v5 fields and add exactly one comment story, three comment
  inventories, and one comment issue array.
- [x] 2.2 Implement the exact source/order/coalescing/ordinal/limit equations,
  shared 511 ordinary-issue cap, shared 1,571,840-byte ordinary string budget,
  terminal collapse, complete discriminated JSON grammar, exact concrete
  structural charge inequalities and table, and the reviewed legal
  ordinary/terminal envelopes derived in the design.
- [x] 2.3 Implement an independent typed protocol-v6 projection with no
  production encoder, sorter, coalescer, budget, terminal, or response-builder
  dependency; include every inherited/new v6 field and ordinary/terminal shape;
  derive the typed response from the request-bound typed package, extraction,
  parser, source, and scan evidence without decoding emitted JSON; add
  recursive dependency audits and inherited/new-field drift witnesses.
- [x] 2.4 Make TypeScript emit and strict-decode v6 only, rejecting unknown
  fields, inconsistent equations, wrong order/cardinality, invalid sources,
  impossible terminal mixtures, and v5 private reports.
- [x] 2.5 Keep public certificate v1 and add co-present optional
  `commentStoryScope`, `commentStory`, `commentInventories`, and
  `commentIntegrityFailures`; preserve legacy v1 decoding and rebuild behavior.

## 3. Evidence and tests

- [x] 3.1 Add compiled selector tests for absent, conventional, relocated,
  external, unsafe, ambiguous, missing, malformed, and wrong-root Comments
  parts, plus unreferenced selected infrastructure and forged absence.
- [x] 3.2 Test reference collection in main, every header/footer role, footnotes,
  and endnotes, including canonical source and event order.
- [x] 3.3 Test missing/malformed/overlong IDs, whitespace/sign/leading-zero
  aliases, negative zero, duplicate definitions, missing definitions,
  non-direct definitions, repeated references, and unreferenced definitions.
- [x] 3.4 Test all semantic crossings, simultaneous-crossing precedence,
  side-wide zero evidence, skipped later sides, and aggregate issue/string
  terminal collapse.
- [x] 3.5 Add strict decoder mutation tests and independent projection drift
  witnesses for every new field, value, ordering, coalescing, budget, and
  terminal category.
- [x] 3.6 Add `ProtocolV6StructuralChargeAudit.lean`,
  `ProtocolV6OrdinaryEnvelopeWitness.lean`, and
  `ProtocolV6CanonicalTerminalShapes.lean` with the exact fixtures required by
  the design.
- [x] 3.7 Extend a real source-derived inplace DOCX triple with references in
  main, selected header/footer, footnote, and endnote stories and one relocated
  Comments part per side; assert nonzero baseline counts before mutations.
- [x] 3.8 Add compared-only mutations for missing relationship/part/definition,
  duplicate/aliased/malformed IDs, relocation, external/ambiguous targets, and
  ancillary-story references without invoking LibreOffice.
- [x] 3.9 Assert one package read, one snapshot write, one extraction, one parse,
  and one bounded scan per selected Comments part; assert that the retained scan
  consumes the request-bound realization, canonical admitted source set, and
  retained source scans and is the identical evidence used by aggregate
  inventory projection and `response.commentParsedEvidence side`; assert
  private-root removal after child close on success, failure, timeout, and
  overflow.
- [x] 3.10 Close checker/decoder issue-shape drift for malformed non-direct
  definitions and reject source or optional extras on terminal comment issues.
- [x] 3.11 Run selected legacy-comment scanning on the complete checked-in
  approximately 41,000-atom NVCA-derived package through the production
  TypeScript-to-compiled-Lean boundary; cover a passing baseline plus
  missing/malformed/overlong nested definition IDs as structured failures under
  the fixed 8 MiB stack.
- [x] 3.12 Replace derived byte/event equality with custom tail-recursive
  checks, prove byte-array and event-sequence equivalence without axioms, and
  execute native plus production-boundary 400,000-byte text/attribute witnesses.
- [x] 3.13 Keep text and attribute values packed as bounded byte arrays through
  typed admission, remove whole-part list conversions from XML/extraction
  scanning, and enforce checker-only near-limit text and attribute peak RSS
  below 1.5 GiB with a 120-second ceiling.

## 4. Coverage and acceptance

- [x] 4.1 Update the checker coverage ledger and exact vendored-schema
  traceability through `CT_Comment`, `CT_TrackChange`, and
  `ST_DecimalNumber`, generalizing the decimal registry title beyond notes,
  without claiming range topology or Microsoft extensions.
- [x] 4.2 Add `@conformance` and structured test citations only where behavior
  exercises §§17.13.4.2, 17.13.4.5, 17.13.4.6, or 17.18.10.
- [x] 4.3 Regenerate conformance and capability projections and update CI paths.
- [x] 4.4 Run strict OpenSpec, spec coverage, citation, conformance-document,
  generated-projection, Lean build/audit, focused, full non-LibreOffice, and
  real-DOCX tests.
- [ ] 4.5 Obtain independent implementation review and post-merge real-document
  smoke before closing #672.
- [x] 4.6 Re-run focused TypeScript tests, full Lean builds and audits, strict
  OpenSpec/spec/conformance checks, and record wall-time/peak-RSS measurements
  for #710 without raising stack limits. Complete-NVCA checker-only evidence:
  1.31 seconds wall time and 306,937,856-byte peak RSS at
  `ulimit -s = 8176` KiB. Near-limit checker-only evidence at an 8192 KiB
  stack: text 1.45 seconds / 1,277,116,416 bytes peak RSS; attribute
  3.32 seconds / 1,314,979,840 bytes peak RSS. The executable acceptance gate
  retains the 120-second timeout and rejects peak RSS at or above 1.5 GiB.

Independent review findings were repaired by #709, but its exact-merge smoke
found that full-document comment scanning exhausts the native stack. Issue #672
was reopened, and #710 tracks the stack-safe full-document acceptance work.

Archive is deferred to #706 so formal Lean theorem evidence is not
misrepresented as TypeScript runtime-test coverage.
