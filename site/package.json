{
  "name": "@usejunior/safe-docx-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Safe DOCX marketing and trust site (Eleventy)",
  "scripts": {
    "build:system-card": "node ../scripts/generate_system_card.mjs --output src/trust/system-card.md",
    "build": "npm run build:system-card && eleventy",
    "dev": "npm run build:system-card && eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
